name: Print Hello World
on:
  push:
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello World"
      - name: Setup by Repo Clone
        uses: actions/checkout@v4
      - name: Steup node version
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
      - name: Greet User
        run: node index.js